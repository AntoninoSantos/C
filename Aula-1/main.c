#include <stdio.h>

int main() {
int n1; n2
  printf("Quantos elem do Vec1?\n");
  scanf("%d", &n1);
  int vec1[n1];
  for(int i=0, i<n1, i++);
  printf("vec1[%d]=", i);
  scanf("%d", 

  printf("Quantos elem do Vec2?\n");
  scanf("%d", &n1);
  int vec1[n1];
  for(int i=0, i<n1, i++);
  printf("vec1[%d]=", i);
  scanf("%d", 

  for(int i=0; i<n1; i++){
    int flag=0;
    for(int j=0, j<i; j++){
      if(vec1[i]==vec1[j]){
        flag=1;
        break;
      }  
    }
    if(flag==0){
      for(int j=0; j<n2; j++){
        if(vec1[i]==vec2[j])
        {
          printf("&d, ", vec1[i]);
          break;
        }
      }
    }
  }
  return=0
}    